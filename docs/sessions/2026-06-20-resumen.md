# Sesión 2026-06-20 — MVP6.1 a producción + rotación de keys

Rama `feat/xray-rentabilidad-historica` → mergeada a `main` (FF) y desplegada en Vercel.
Validación en cada push: `npm run verify` 9/9 (tsc + 49 tests + Aurum-077).

## Hitos

### Modelo de rentabilidades (MVP6.1)
- **TWR encadenado gap-aware** (`FRM-007`/`FRM-008`) + **serie temporal** + **horizontes** (`FRM-014`, D30/D31) en `lib/returns.ts`. Port fiel de `metrics.py`. Tests añadidos.
- **Panel "Rentabilidad"** en el dashboard: Simple / MWR / TWR (acumulada + anualizada) + **gráfico interactivo de evolución** (líneas con chips/tooltip) + **barras por periodo** (YTD/1A/3A/5A/SI, selector métrica + toggle anualizada).
- MWR ya era IRR real; TWR cableado nuevo.

### X-Ray
- Columnas **5 y 10 años anualizado** en la tabla de fondos.
- Columna **10A an.** en la matriz histórica por año (2016–2025).

### Fix de datos
- **Paginación del histórico de posiciones** (`getPositionHistory`/`getAggregatedHistory`): Supabase cortaba a 1000 filas → el gráfico se truncaba en ~2023 y disparaba la alerta de integridad. Resuelto con `.range()` paginado.

### Carga de datos (Supabase prod)
- **Operaciones** unificadas: 11.535 (+1.084, fusión de 2 informes CM).
- **Serie completa de snapshots**: 411 Pos + 411 Saldo (2019-11 → 2026-06-12). Último snapshot pasa a 2026-06-11.
- **Universo de fondos v6** (`universo_mstarpy_v6.xlsx`, con `rent_YYYY` por año). Nota: el `_clean` quita las columnas por año → cargar el v6 sin limpiar.
- Taxonomía **PEND-017** (3 tipos nuevos: RECEPCION IIC LIBRE PAGO→PLUS, 2 fusiones fiscales→NEUTRO).

### Validador
- Referencia Aurum-077 actualizada a **junio**: invertido 294.722,47 · valor 394.734,48 · ganancia 100.012,01 (33,93%) · saldo 155,68. Confirmado por `metrics.py`. (Antes marzo/290.922,76 con data incompleta.)

### Seguridad
- **Rotación de keys Supabase COMPLETADA** (legacy→publishable/secret): migradas local/.env, pipeline `.env`, Vercel; verificado prod (login, admin KPIs, X-Ray) + local 9/9; **legacy desactivada → SEC-1 cerrado**. La service_role filtrada ya no vale.
- Quitadas **claves y contraseña hardcodeadas** de `seed-data.mjs`/`test-auth.mjs` (ahora leen de `.env.local`). `.env*.save` añadido a `.gitignore`.
- Nuevo: comando `npm run verify` (gate) + `npm run scan` (Semgrep, Trail of Bits + OWASP) + **puerta de acceso** owner/admin (`CLIENTS_ENABLED=false`).

## Decisiones
- Referencia de Aurum es **móvil** (entrarán Excels desde 2019 → seguirá cambiando). No es hand-calc fijo de Edgard.
- TWR es **aproximado** (snapshots semanales, D9) y **gap-aware** (V-010).
- Dashboard **solo owner/admin** hasta extender a clientes (flag de una línea).
- Deploy directo `feat`→`main` por fast-forward (sin PR), solo dev.

## Pendiente
- 🔑 **Edgard cambia su contraseña** (quedó en el historial de git; la rotación de keys NO la invalida).
- 🧹 Código: MINUS a spec (D6) en producción; check de rol también en `middleware.ts` + log de acceso; codificar SEC-2 ("credenciales nunca en chat") en `guidelines/security.md`.
- WIP footer de versión (`next.config.mjs`, `lib/version.ts`, `diagnose-flows.mjs`) sin commitear — commit aparte cuando esté.
- Pendiente de Edgard: docs `02_data_model.md` / `04_pipeline.md`; decisión "fondos sin histórico" para medias ponderadas a 10 años.
- `npm audit`: 11 vulns (mayoría `xlsx` sin fix); revisar.

### [tarde] - Marcado TWR no fiable (PEND-018/V-012) + harness de verificación 20 CV
- Origen: Edgard reporta "las rentabilidades no dan bien" (WhatsApp 17:43). Diagnóstico: el correo de estado del 20/06 sobrevendió (TWR sin marcar, X-Ray en placeholder, "contrastado" = solo Aurum-077).
- Construido SOLO con input de Edgard (paquete 260619):
  - `lib/returns.ts`: `assessTwrReliability()` + `TwrResult.maxAbsSubperiodReturn`. Criterio del manual v1.1 §1.4: no fiable si TWR null, o |R^MD| de algún sub-periodo ≥ 100%, o |TWR_anual − MWR_anual| > 20pp.
  - `lib/__tests__/fixtures/model-cases.json` + `docs/model_reference_2026-06-12.json`: referencia de las 20 CV (M-01..M-19b) generada con `manual_metrics.py` de Edgard sobre la Raw data CM. Regenerable con `scripts/generate-model-reference.py`.
  - `lib/__tests__/twr-reliability.test.ts`: harness que corre returns.ts sobre las 20 CV y compara CI/Simple/MWR/TWR + flag.
  - `ClientDashboard.tsx`: panel TWR muestra "n/d · no fiable · usa MWR" en los casos marcados, con nota explicativa.
- Verificación (harness standalone, vitest no corre en sandbox por binario nativo): **94/94 OK**. `npx tsc --noEmit` exit 0.
- Hallazgo: el `chainedTwr` del dashboard YA reproduce el TWR del manual (boundary inclusivo en t0 ≈ stub-MW); el `manual_metrics.py` del paquete explota en inception (M-02 743% vs 35% del manual). El marcado coincide EXACTO con los 9 del manual (M-04,05,10,13,14,15,16,17,18).
- Pendiente: correr `npm run verify` (vitest + validate-aurum077) en local antes de commit/push. Sin commit ni push en esta sesión.
- NO tocado (riesgo / depende de Edgard): cutover MINUS→D6 en producción; stub-MW (engine actualizado de Edgard); X-Ray cartera real.
