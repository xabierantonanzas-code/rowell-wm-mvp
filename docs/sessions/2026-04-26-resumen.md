# Sprint 0 MVP7 - Pre-arranque (26/04/2026)

Sesión enfocada en limpiar deuda técnica MVP6 antes de abrir MVP7.

### [14:51] - Tag mvp6-final creado
- Commands: `git tag -a mvp6-final 9c5f997` + `git push origin mvp6-final`
- Archivos tocados: ninguno (solo refs git)
- Resultado: OK. Tag publicado en GitHub junto a `pre-mvp6-v2`.
- Razón: marca de release MVP6 que faltaba (versionado incompleto detectado en inventario).

### [14:57] - Tipado de getAllOperationsForAccounts
- Commit: `6d9dfe7` chore(types): type getAllOperationsForAccounts as Operation[]
- Archivos: `lib/queries/operations.ts` (+7/-3)
- Resultado: OK. tsc clean, 20/20 tests passing, callers transparentes.
- Decisión: cast en el boundary con comentario inline en lugar de migrar
  `createServerClient<any>` a `createServerClient<Database>`. Mantiene el
  patrón del repo. Cuando se migre el cliente, el cast se elimina sin tocar callers.

### [15:13] - Toggle TWR/MWR ocultado hasta MVP6.1
- Archivos: `components/dashboard/ClientDashboard.tsx` (bloque L1400-1432
  envuelto en `HIDDEN UNTIL MVP6.1`), `docs/MVP6.1_TODO.md`
  (criterios de aceptación TWR ampliados).
- Resultado: OK. tsc clean, 20/20 tests passing. Línea del chart sigue
  pintando TWR-subperiodo (default), solo se oculta el selector de UI.
- Decisión: Opción C diferida con razón explícita. Política: no mostrar
  TWR si no se garantiza integridad del input (snapshots completos,
  fechas consistentes, taxonomía validada). NO es deuda técnica — es
  salvaguarda de confianza del cliente: preferible "datos insuficientes"
  que un número falso. Se reactiva cuando se cumplan los criterios de
  aceptación listados en docs/MVP6.1_TODO.md.
- State vivo intencionalmente: `returnMethod`, useMemos `twrPeriods`/
  `mwrPeriods`, bifurcación en `chartData.map(...)` (~L1253). No se borran
  porque MVP6.1 reescribe TWR de cero.
